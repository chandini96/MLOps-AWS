# Consolidated requirements for all components
# Install all dependencies needed across the MLOps pipeline

# Core data handling
pandas>=1.5.0
numpy>=1.23.0

# HTTP requests (for data_fetcher)
requests>=2.28.0

# Machine Learning (for preprocess, train, evaluate)
scikit-learn>=1.2.0

# Model persistence (for train, evaluate, model_registry)
joblib>=1.2.0

# Visualization (for evaluate)
matplotlib>=3.6.0
seaborn>=0.12.0

# AWS (for train, model_registry)
boto3>=1.26.0

# SageMaker SDK (for pipeline orchestration)
sagemaker>=2.100.0
